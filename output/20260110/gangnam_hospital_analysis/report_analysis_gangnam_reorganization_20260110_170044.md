# 분석 리포트: 강남 병원 크롤링 파일 정리 계획

- **작성일자**: 2026-01-10 17:00:44
- **대상 디렉토리**: `crawling/gangnam`
- **분석 목적**: 혼재된 스크립트 및 데이터 파일의 체계적 정리 및 관리 효율화

## 1. 현재 파일 현황 분석
현재 `crawling/gangnam` 디렉토리에는 다음과 같이 총 46개의 파일이 혼재되어 있습니다.

| 구분 | 파일 개수 | 주요 파일 예시 |
| :--- | :--- | :--- |
| **Python 스크립트** | 12개 | `gangnam_crawling_*.py`, `extract_*.py` |
| **마크다운 리포트** | 13개 | `error_report_*.md`, `report_*.md` |
| **데이터 파일 (CSV)** | 19개 | `gangnam_hospitals_*.csv`, `*reviews*.csv` |
| **문서/가이드** | 2개 | `hospital crawling_step*.md` |

### 문제점
- `data/` 폴더 내에 데이터뿐만 아니라 스크립트와 리포트가 함께 존재함.
- `extraction/` 폴더가 별도로 존재하여 관리가 파편화됨.
- 원본 데이터, 중간 결과물, 최종 결과물이 구분 없이 섞여 있음.

## 2. 제안하는 정리 체계 (To-Be)

| 폴더명 | 용도 | 이동 대상 성격 |
| :--- | :--- | :--- |
| `scripts/` | 실행 스크립트 | 모든 `.py` 파일 |
| `reports/` | 분석 결과 리포트 | 분석 결과 요약 및 에러 분석 `.md` |
| `docs/` | 프로젝트 문서 | 진행 단계, 가이드라인 `.md` |
| `data/raw/` | 초기 데이터 | 수집 직후의 변조되지 않은 CSV |
| `data/processed/` | 가공 데이터 | 필터링, 정제 완료된 최종 CSV |
| `data/archive/` | 아카이브 | 중간 과정 백업 및 재개용 CSV |

## 3. 상세 이동 계획 (검토안)

### 3.1 스크립트 및 문서
- **scripts/**: `data/`와 `extraction/` 폴더의 모든 `.py` 파일을 이관
- **reports/**: `data/`와 `extraction/` 폴더의 모든 리포트 성격 `.md` 파일을 이관
- **docs/**: 가이드라인 및 단계별 매뉴얼 이관

### 3.2 데이터 분류 상세
- **data/raw/**: `gangnam_hospitals_20260104_`로 시작하는 초기 수집본
- **data/processed/**: `FULL`, `final`, `FINAL_ALL`, `skin_filter` 등의 키워드가 포함된 최종 가공본
- **data/archive/**: `RESUME`, `detail` 키워드가 포함된 중간 기록용 파일

## 4. 기대 효과
- **가독성 향상**: 각 파일의 역할을 폴더 구조만으로 파악 가능
- **데이터 무결성**: 원본과 가공 데이터를 분리하여 실수에 의한 데이터 오염 방지
- **유지보수 용이**: 특정 스크립트나 리포트를 찾는 시간 단축

## 5. 특이 사항 및 에러 리포트
- **진행 과정 에러**: 특이사항 없음. 파일 목록 분석 결과 `data/` 폴더 내에 파일이 과도하게 집중된 것을 확인.
- **API 사용 정보**: 본 분석 작업에는 외부 API를 사용하지 않았으므로 비용이 발생하지 않음 (예상 비용: 0원).

---
*본 리포트는 글로벌 규칙에 따라 `output/reports/` 폴더에 저장되었습니다.*
